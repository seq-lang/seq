# 786

import itertools
import time

y = time.time()
print [i for i in itertools.combinations('ABCD', 2)]
print time.time()-y
# print list(iter(itertools.combinations('ABCD', 2))) # <- fails
# print list(itertools.combinations('ABCD', 2)) # <- fails

try:
	print 'hohoi'
finally:
	print 'fin'

