import math

NAN = math.nan
INF = math.inf
NINF = -INF

print math.cosh(0.0) == 1
print math.cosh(2.0)-2*math.cosh(1.0)**2 == -1
print math.cosh(INF) == INF
print math.cosh(NINF) == INF
print math.isnan(math.cosh(NAN)) == True
