import math

NAN = math.nan
INF = math.inf
NINF = -INF

print math.sin(0.0) == 0
print math.sin(math.pi/2) == 1
print math.sin(-math.pi/2) == -1
print math.isnan(math.sin(INF)) == True
print math.isnan(math.sin(NINF)) == True
print math.isnan(math.sin(NAN)) == True
