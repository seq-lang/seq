import math

NAN = math.nan
INF = math.inf
NINF = -INF

print math.atan(-1.0) == -math.pi/4
print math.atan(0.0) == 0
print math.atan(1.0) == math.pi/4
print math.atan(INF) == math.pi/2
print math.atan(NINF) == -math.pi/2
print math.isnan(math.atan(NAN)) == True
