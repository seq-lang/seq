import math

NAN = math.nan
INF = math.inf
NINF = -INF

print math.tanh(0.0) == 0
print math.tanh(1.0) + math.tanh(-1.0) == 0
print math.tanh(INF) == 1
print math.tanh(NINF) == -1
print math.isnan(math.tanh(NAN)) == True
