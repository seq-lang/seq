import math

NAN = math.nan
INF = math.inf
NINF = -INF

print math.lgamma(1.0) == 0.0
print math.lgamma(2.0) == 0.0
print math.lgamma(-1.0) == INF
print math.lgamma(INF) == INF
print math.lgamma(NINF) == INF
print math.isnan(math.lgamma(NAN)) == True
