argv = list(__argv__, len(__argv__))


cimport seq_stdin() -> ptr[byte]
stdin = File(seq_stdin())

cimport seq_stdout() -> ptr[byte]
stdout = File(seq_stdout())

cimport seq_stderr() -> ptr[byte]
stderr = File(seq_stderr())

cimport exit(int)
