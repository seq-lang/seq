cimport log(x: float) -> float
cimport exp(x: float) -> float
cimport erf(x: float) -> float
cimport sqrt(x: float) -> float

def factorial(x: int) -> int:
    r = 1
    for i in range(2, x + 1):
        r *= i
    return r
